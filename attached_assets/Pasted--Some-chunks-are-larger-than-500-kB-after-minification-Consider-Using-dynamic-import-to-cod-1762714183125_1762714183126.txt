(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rollupOptions.output.manualChunks to improve chunking: https://rollupjs.org/configuration-options/#output-manualchunks
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 14.35s

  dist/index.js  1003.0kb

⚡ Done in 52ms
▲ [WARNING] Expected "}" to go with "{" [css-syntax-error]
❌ CSS STILL BROKEN
[root@wizardly-swirles NaviMed]# cd /var/www/vhosts/navimedi.org/httpdocs/NaviMed

# Show lines 690-710 to see the exact problem
echo "=== Lines around error (690-710) ==="
sed -n '690,710p' client/src/index.css

# The problem is unclosed @media blocks. Let me add the missing closing braces
# Fix line 698-702 area by adding the missing }
sed -i '702 i\}' client/src/index.css

# Rebuild and test
npm run build 2>&1 | grep -i "syntax-error" && echo "❌ STILL BROKEN" || echo "✅ FIXED!"

bash post-build.sh
pm2 restart navimed
=== Lines around error (690-710) ===
.max-w-7xl {
  max-width: 1600px !important;
/* Add responsive padding to all mx-auto containers */
.mx-auto {
  padding-left: 1rem !important;
  padding-right: 1rem !important;
@media (min-width: 768px) {
  .mx-auto {
    padding-left: 2rem !important;
    padding-right: 2rem !important;
  }
▲ [WARNING] Expected "}" to go with "{" [css-syntax-error]
❌ STILL BROKEN
📦 Syncing built assets from dist/public to server/public...
✅ Assets synced successfully with rsync!
   dist/public → server/public
   21 files synced
Use --update-env to update environment variables
[PM2] Applying action restartProcessId on app [navimed](ids: [ 0 ])
[PM2] [navimed](0) ✓
┌────┬────────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┬──────────┬──────────┐
│ id │ name       │ namespace   │ version │ mode    │ pid      │ uptime │ ↺    │ status    │ cpu      │ mem      │ user     │ watching │
├────┼────────────┼─────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┼──────────┼──────────┼──────────┼──────────┤
│ 0  │ navimed    │ default     │ 1.0.0   │ fork    │ 1917596  │ 0s     │ 30   │ online    │ 0%       │ 12.5mb   │ root     │ disabled │
└────┴────────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┴──────────┴──────────┘
[root@wizardly-swirles NaviMed]# 